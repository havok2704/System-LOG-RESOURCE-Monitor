# System-LOG-RESOURCE-Monitor
A bash-based system log and resource monitor that collects system usage data, detects anomalies, and sends alerts for high CPU, RAM, disk usage, and critical log entries.
